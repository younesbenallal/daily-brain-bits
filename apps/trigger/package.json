{
	"name": "@daily-brain-bits/trigger",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "set -a && source .env && set +a && npx trigger.dev dev",
		"deploy": "set -a && source .env && set +a && npx trigger.dev deploy",
		"type-check": "tsc --noEmit --incremental",
		"clean": "rm -rf dist"
	},
	"dependencies": {
		"@daily-brain-bits/back": "workspace:*",
		"@sentry/node": "^10.38.0",
		"@trigger.dev/sdk": "4.3.3"
	},
	"devDependencies": {
		"@daily-brain-bits/typescript-config": "workspace:*",
		"@sentry/esbuild-plugin": "^4.8.0",
		"@trigger.dev/build": "4.3.3",
		"typescript": "^5.6.0"
	}
}
